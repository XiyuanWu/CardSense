# CardSense backend environment variables
# Copy to .env and fill in values:
#   cp .env.example .env

# Google Gemini — required for AI chat assistant (/api/chat/)
# Get a key: https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
